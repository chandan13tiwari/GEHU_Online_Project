<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sourcey.materiallogindemo">

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.READ_SMS"/>
    <uses-permission android:name="android.permission.RECEIVE_SMS"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">


        <activity android:name=".Splashscreen"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>




        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".RegForms"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".Feedback" />
        <activity
            android:name=".LectureVideo"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".Note"
            />

        <activity
            android:name=".VerifyPhoneActivity"
            android:theme="@style/AppTheme.Dark" />

        <activity
            android:name=".SemRegistration"
            android:theme="@style/AppTheme.Dark" />

        <activity
            android:name=".OutScreen"
            android:theme="@style/AppTheme.Dark" />

        <activity
            android:name=".AdminActivity"
            android:theme="@style/AppTheme.Dark" />

        <activity
            android:name=".AdminScreen"
            android:theme="@style/AppTheme.Dark" />

        <activity
            android:name=".PaytmPaymentSample"
            android:theme="@style/AppTheme.Dark" />

        <activity
            android:name=".StudentDetails"
            android:theme="@style/AppTheme.Dark" />

        <activity
            android:name=".ViewPDFFile"
            android:theme="@style/AppTheme.Dark" />




        <activity
            android:name=".Homepage"
            android:label="Dashboard"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>